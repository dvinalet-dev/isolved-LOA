# isolved LOA Tracker

React + Vite. In-session data only (no database yet).

## Local
npm install
npm run dev        # http://localhost:3000

## Container
Docker builds the app and serves it on port 3000 (see Dockerfile).
